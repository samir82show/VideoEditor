/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

import com.rawoona.videoeditor.entity.Video;
import com.rawoona.videoeditor.entity.VideoController;
import java.io.IOException;
import java.text.ParseException;
import javax.mail.MessagingException;
import org.junit.After;
import org.junit.AfterClass;
import org.junit.Assert;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;

/**
 *
 * @author Samir
 */
public class VideoControllerTest {

    public VideoControllerTest() {
    }

    @BeforeClass
    public static void setUpClass() {
    }

    @AfterClass
    public static void tearDownClass() {
    }

    @Before
    public void setUp() {
    }

    @After
    public void tearDown() {
    }

    @Test
    public void generateRandomNameTest() throws ParseException {
//        VideoController videoController = new VideoController();
//        videoController.getVideo().setName("10- Vanishing  Exploding gradients.mp4");
//        Assert.assertTrue(videoController.generateRandomName(), false);
    }

    @Test
    public void convertSecondsToTimeTest() throws IOException, MessagingException {
//        VideoController videoController = new VideoController();
//        Assert.assertEquals("testing 0 second " + videoController.convertSecondsToTime(0), "00:00:00", videoController.convertSecondsToTime(0));
//        Assert.assertEquals("testing 1 second " + videoController.convertSecondsToTime(0), "00:00:01", videoController.convertSecondsToTime(1));
//        Assert.assertEquals("testing 15 seconds " + videoController.convertSecondsToTime(15), "00:00:15", videoController.convertSecondsToTime(15));
//        Assert.assertEquals("testing 60 second " + videoController.convertSecondsToTime(60), "00:01:00", videoController.convertSecondsToTime(60));
//        Assert.assertEquals("testing 90 second " + videoController.convertSecondsToTime(60), "00:01:30", videoController.convertSecondsToTime(90));
//        Assert.assertEquals("testing 3600 second " + videoController.convertSecondsToTime(3600), "01:00:00", videoController.convertSecondsToTime(3600));
//        Assert.assertEquals("testing 3611 second " + videoController.convertSecondsToTime(3611), "01:00:11", videoController.convertSecondsToTime(3611));
//        Assert.assertEquals("testing 3671 second " + videoController.convertSecondsToTime(3671), "01:01:11", videoController.convertSecondsToTime(3671));
//        Assert.assertEquals("testing 13028 second " + videoController.convertSecondsToTime(3671), "03:37:08", videoController.convertSecondsToTime(13028));
    }

}
